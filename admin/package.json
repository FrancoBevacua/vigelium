{
  "name": "vigelium-administracion",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "node scripts/sync-shared.mjs && vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "react": "19.2.3",
    "react-dom": "19.2.3",
    "jpeg-js": "0.4.4",
    "pako": "2.2.0"
  },
  "devDependencies": {
    "vite": "8.3.0"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}